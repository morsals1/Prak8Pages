<Page x:Class="WpfP7.Pages.MainUserPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="MainUserPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Информация о докторе" Margin="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding CurrentDoctor.LastName}" FontWeight="Bold" Margin="5"/>
                <TextBlock Text="{Binding CurrentDoctor.Name}" Margin="5"/>
                <TextBlock Text="{Binding CurrentDoctor.MiddleName}" Margin="5"/>
                <TextBlock Text="-" Margin="5"/>
                <TextBlock Text="{Binding CurrentDoctor.Specification}" Margin="5"/>
                <TextBlock Text="ID:" Margin="5"/>
                <TextBlock Text="{Binding CurrentDoctor.Id}" Margin="5"/>
            </StackPanel>
        </GroupBox>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
            <Button Content="Создать пациента" Click="CreatePatientButton_Click" Margin="5" Padding="10,5"/>
            <Button Content="Начать прием" Click="StartAppointmentButton_Click" Margin="5" Padding="10,5"/>
            <Button Content="Изменить информацию" Click="EditInfoButton_Click" Margin="5" Padding="10,5"/>
        </StackPanel>

        <ListView Grid.Row="2" ItemsSource="{Binding Patients}" SelectedItem="{Binding SelectedPatient}"
                  MouseDoubleClick="PatientList_DoubleClick" Margin="10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <TextBlock FontWeight="Bold" Text="{Binding LastName}"/>
                        <TextBlock Text="{Binding Name}"/>
                        <TextBlock Text="{Binding MiddleName}"/>
                        <TextBlock Text="{Binding PhoneNumber}" FontSize="10" Foreground="Gray"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>